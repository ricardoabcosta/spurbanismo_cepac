# =============================================================================
# CEPAC — Dependências de teste
# Instalar: pip install -r requirements.txt -r requirements-test.txt
# =============================================================================

# Test runner + asyncio support
pytest==8.3.4
pytest-asyncio==0.23.8
pytest-cov==5.0.0

# Async HTTP client para testes de integração FastAPI
httpx==0.27.2

# Testcontainers PostgreSQL — banco real para testes de integração
testcontainers[postgresql]==4.8.1

# asyncpg para executar migrations no container de teste
asyncpg==0.30.0
